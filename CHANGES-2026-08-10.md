# AFS Admin — Session Log (2026-08-07 ~ 2026-08-10)

## 개요

Utility Bills 페이지 전면 개편 세션. 기존 Bills/Calendar/Vendors/Analytics 4-tab 구조를 Dashboard 중심으로 재설계하고, 청구서 상세 열람·수정(노트/상태/부분결제/연체이자) 기능을 추가했으며, 그 과정에서 드러난 실데이터 오류(중복 벤더, 잘못된 날짜, 잘못된 연체 태그 등)를 다수 정리했습니다. Vendor 관리 모달에도 Payment Methods/Bills 탭을 추가했고, 로그인 시 화면 깜빡임 버그도 고쳤습니다.

커밋 15개 (`c654bd4` ~ `50eba17`), 전부 `app/utility/page.tsx` / `app/utilities/vendors/page.tsx` / `app/providers.tsx` / `lib/billStatus.ts` 대상. 이 외에 코드 변경 없이 Supabase `utility_bills` 데이터만 직접 수정한 정리 작업이 다수 포함됨 (아래 8번 섹션).

---

## 1. Utility Bills 구조 개편 — Dashboard/All Bills/Analytics 3-tab

### 커밋: `c654bd4`, `2b39464`

기존 Bills/Calendar/Vendors/Analytics 4개 탭 중 Calendar·Vendors는 사이드바에 이미 동일 기능의 별도 페이지가 있어 중복이었음 (그 안의 구버전 `CalendarTab`/`VendorTab` 구현은 삭제). Vendors 페이지 기본 회사 필터를 All → AFS로 변경. 사이드바의 "Company / Location" 트리(더미 데이터, 미사용)도 삭제.

새 구조:
- **Dashboard** (기본 화면): Overdue/Current 청구서 리스트 + Vendor별 청구 이력 아코디언
- **All Bills**: 기존 전체 테이블 + 상태 필터 칩 (그대로 유지)
- **Analytics**: 기존 그대로 유지

| 파일 | 내용 |
|------|------|
| `app/utility/page.tsx` | `DashboardTab` 신규 컴포넌트, 구버전 `CalendarTab`/`VendorTab` 삭제 |
| `app/components/Sidebar.tsx` | "Company / Location" 더미 트리 삭제 |
| `app/utilities/vendors/page.tsx` | `coFilter` 기본값 `'all'` → `'afs'` |

---

## 2. Dashboard 상세 기능

### 커밋: `de99b86`, `10b7e23`, `6ff80f6`, `d620786`

| 내용 | 설명 |
|------|------|
| 청구서 행 클릭 → 상세 펼치기 | Bill #, Account, Issued/Due, Payment, Notes, 상태 드롭다운, 수정/삭제 버튼을 인라인으로 열람·수정 (`BillExpandPanel`) |
| 월 표기 | 상대 표기("2 months ago") → 절대 표기("Aug 2026")로 통일 |
| 노트 편집 | 싱글클릭 → **더블클릭**해야 편집모드 진입, 입력창 폭 확장 |
| 폰트 확대 | Dashboard 탭 전반 텍스트 한 단계씩 확대 |
| Total/Current/Overdue 통계박스 → 실제 리스트로 교체 | 숫자만 보여주던 3박스 삭제, All Bills의 Overdue 알림판과 같은 스타일로 실제 청구서 목록(뱃지·업체명·금액·마감일·Paid 버튼) 표시. 회사 필터와 무관하게 항상 전체 표시 |
| 계정번호 표시 | 계정이 여러 개인 벤더(예: Fontana Water 3개 계정)는 청구서 행 앞에 계정번호 표기, 계정 1개인 벤더는 생략 |

---

## 3. Grandbridge 중복 벤더 데이터 정리 (DB만, 커밋 없음)

TNT의 "Grandbridge"(레거시 AI-SQL 입력, 노트에 원본 파일명 없음)와 "GrandBridge Energy"(신규 ingestor 파이프라인, 원본 파일명 있음)가 동일 5개월치 청구서를 중복 보유하고 있던 것을 발견.

- 레거시 "Grandbridge" 5건 삭제
- "GrandBridge Energy" 5건을 "Grandbridge"로 이름 통일 (원본 파일명 있는 쪽을 남김)
- 그 중 상태가 밀려있던 1건(6월/7월 마감분)을 Paid로 정정

---

## 4. Vendor 편집 모달 — Payment Methods / Bills 탭 추가

### 커밋: `04a04e1`

| 탭 | 내용 |
|----|------|
| Payment Methods | 해당 벤더 소속 회사(AFS/TNT/ZFS)의 결제수단을 모달 안에서 바로 조회·추가·삭제 (기존 `payment_methods` 테이블 재사용) |
| Bills | 벤더 이름으로 매칭되는 청구서 목록 열람 (읽기 전용 — 업체명·계정·마감일·금액·상태) |

---

## 5. 부분결제 / 연체이자 입력 기능

### 커밋: `f1ac0af`, `91e5dc9`

- 상태 드롭다운에 결제 기록 모달(`PartialPaymentModal` → "Record Payment") 추가: **Amount Paid / Payment Date / Late Fee(연체이자)** 입력, 잔액 자동 계산
- 드롭다운 메뉴가 Vendor 아코디언 카드의 `overflow-hidden`에 잘려서 하단 옵션(Partially Paid…, Carried Forward)이 안 보이던 버그 → `document.body`에 portal 렌더링 + 뷰포트 하단 근처면 위쪽으로 자동 flip 되도록 수정

---

## 6. "Paid (Late)" 상태 — 연체 후 결제된 청구서 구분

### 커밋: `80646ba`, `3a5a13a`(무관, 아래 7번), `65553bc`, `5fd1e16`, `50eba17`

- `lib/billStatus.ts`: 마감일 이후에 결제된 청구서는 단순 "Paid"가 아니라 **"Paid (Late)"**로 표시되고, Overdue 목록에서는 제외 (`isActiveOutstanding`엔 포함 안 됨)
- 완납 금액을 결제모달에 입력하면 자동으로 `paid` 상태로 승격 (전에는 `partially_paid`에 영구히 멈춰서 Overdue에 계속 남는 버그가 있었음)
- **버그 발견·수정**: "Paid" 버튼(간단 처리)이 결제일(`paid_at`)을 항상 "지금 이 순간"으로 저장하던 구조적 버그 → 백필된 옛날 청구서는 "지금"이 항상 마감일보다 늦어서 몇 번을 눌러도 "Paid (Late)"에서 못 벗어남. **간단 "Paid" 버튼은 이제 결제일을 기록하지 않도록 수정** (결제일은 오직 "Record Payment" 모달에서만 기록)
- 드롭다운의 "Partially Paid…"와 "Paid (Late)…" 항목을 **"Paid (Late) / Partial…"** 하나로 통합

---

## 7. 로그인 화면 깜빡임 수정

### 커밋: `3a5a13a` (+ 이전 커밋)

`app/providers.tsx`의 계정별 권한(`allowedSections`) 조회 로직 버그:
1. 로그인 확인이 끝나기 전에 권한 조회 effect가 `user === null` 상태로 먼저 실행되어 "전체 권한"으로 오판 → 사이드바/콘텐츠가 잠깐 보였다가 로딩화면으로 복귀
2. Supabase가 토큰 갱신·탭 포커스 등으로 `onAuthStateChange`를 반복 발생시킬 때마다 (같은 계정인데도) user 객체 참조가 새로 생성되어 권한 재조회 → loading 반복 토글 → 반복 깜빡임

**수정**: 권한 조회 effect가 로그인 확인 완료 후에만 실행되도록 가드 추가, 의존성을 user 객체 참조 대신 **이메일(고정 primitive)**로 변경.

---

## 8. 실데이터 정리 기록 (코드 변경 없음, Supabase 직접 수정)

| 내용 | 건수 |
|------|------|
| Orkin(ZFS Pest Control) `issue_date`가 6건 전부 `2026-07-10`으로 동일하게 잘못 들어가 있던 것 발견 → Due Date에 있던 실제 인보이스 날짜를 Issue Date로 옮기고 Due Date는 비움 | 6건 |
| `amount_paid`가 이미 총액과 같은데 `balance_status`만 `partially_paid`로 멈춰서 Overdue에 계속 남아있던 청구서 (Rogers, Fontana Water 등) | 8건 |
| 위 5번 버그로 인해 `paid_at`이 실제 결제일이 아니라 처리 시점(주로 이전 세션의 대량 정리 작업 시각)으로 찍혀서 "Paid (Late)"로 잘못 표시되던 청구서 → `paid_at` 초기화 | 47건 |

---

## 미해결 / 참고사항

- **Cambridge Water 2건**은 실제로 잔액이 남아있는 진짜 부분결제라 Overdue 목록에 그대로 유지 (완납-지연 건만 Overdue에서 제외하는 것으로 판단 — 잔액 남은 것도 빼길 원하면 별도 요청 필요)
- Orkin 청구서의 가격 불일치 1건(Bill #300271228, 원래 `current_charges` 683.58 vs `amount` 227.86)은 사용자가 직접 원본 PDF 확인 후 수정 완료
- 이번 세션 마지막 커밋(`50eba17`)의 드롭다운 라벨 변경은 로그인 세션 만료로 브라우저 재확인 없이 배포됨 (타입체크만 통과 확인)

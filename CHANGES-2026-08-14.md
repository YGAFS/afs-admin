# AFS Admin — Session Log (2026-08-14)

## 개요

앱 전체에 흩어져 있던 파스텔 배지/이모지/저대비 구분선 스타일을 하나의 절제된 디자인 시스템으로 통일한 세션. Utility Dashboard에서 시작해서 Utility 섹션 전체(Bills/Vendors/Locations/Reports/Documents), HR 메인 대시보드, 출근부 그리드까지 순차적으로 확장 적용하고 각 단계마다 배포까지 완료함.

---

## 1. 배경 — 왜 다시 손봤나

기존 화면들은 Tailwind 기본 팔레트를 섹션마다 즉흥적으로 골라 쓴 상태였음:
- 구분선이 `border-gray-100`/`200`이라 저해상도 모니터에서 거의 안 보임
- 카드마다 `bg-{color}-50` 파스텔을 무작위로 사용해 "AI가 만든 기본 템플릿" 느낌
- 이모지 아이콘 남발 (💰📊⚠️📅✅🎉 등)
- 상태 배지가 색깔별로 제각각이라 일관성 없음

참고 레퍼런스로 토스증권 UI를 제시받아 방향을 잡음: 거의 무채색 기반 + 신호색(빨강/파랑)은 오직 실제 의미(연체/완납)에만 최소한으로 사용, 버튼은 검정 알약 하나로 통일.

---

## 2. 디자인 토큰 확정 (`app/globals.css`)

Tailwind v4 `@theme`에 공용 토큰을 추가해서 이후 모든 화면이 같은 값을 참조하도록 함:

```css
--color-ink: #191f28;         /* 기본 텍스트 */
--color-ink-muted: #6b7280;   /* 보조 텍스트 */
--color-ink-faint: #9ca3af;   /* 힌트/캡션 */
--color-line: #d1d5db;        /* 기본 구분선 */
--color-line-soft: #e5e7eb;   /* 옅은 구분선 */
--color-line-strong: #9ca3af; /* 강한 구분선 (출근부 그리드 외곽/헤더용, 나중에 추가) */
--color-pill: #f2f4f6;        /* 중립 배경 (뱃지/버튼/hover) */
--color-signal-neg: #f04452;  /* 연체/위험 신호 (텍스트 전용, 배경 채움 금지) */
--color-signal-pos: #3182f6;  /* 완납/긍정 신호 (텍스트 전용) */
```

원칙: 상태색은 배경을 채우는 배지가 아니라 텍스트 색으로만 표현. 버튼은 기본적으로 검정(`bg-ink`) 하나로 통일하고, 색은 신호가 있을 때만 사용.

---

## 3. Utility Dashboard (Overview + Calendar)

- `app/utilities/overview/page.tsx`: Stat card 5개를 동일 크기로 나열하던 걸 Overdue 카드만 2배 크게(hero) 키우고 나머지는 보조 정보로 축소. 도넛/바 차트 색상 정리, 이모지 아이콘 전부 제거.
- `app/utilities/calendar/page.tsx`: 셀 안에 색깔 배지를 여러 개 쌓던 방식 → 점(dot) + 텍스트로 축약, 그리드 라인 대비 강화.
- `app/utilities/components/UtilityShell.tsx`: 헤더 톤 정리.

**커밋**: `6685dc3`

---

## 4. Utility 섹션 전체 (Bills/Vendors/Locations/Reports/Documents)

- `app/utility/page.tsx` (2,467줄, `/utilities/bills`와 공유): Dashboard/All Bills/Analytics 3개 탭 + Payment Methods/Add Credit 모달까지 전부 정리. 액션 아이콘(📎💰✏️🗑)을 "File/Edit/Del" 텍스트 라벨로 교체.
- `lib/billStatus.ts`의 공용 `STATUS_BADGE`도 같은 방식으로 변경 (배경 채움 → 텍스트 색만) — Vendors/Calendar 등 이 상수를 같이 쓰는 화면도 자동으로 톤이 맞춰짐.
- `app/utilities/vendors/page.tsx` (1,483줄): 회사 배지, 연락처, 결제수단, 문서 링크 섹션 전부 정리.
- `app/utilities/locations/page.tsx`, `app/utilities/reports/page.tsx`, `app/utilities/documents/page.tsx`: 같은 톤으로 정리.

**커밋**: `6685dc3`

---

## 5. HR 메인 대시보드

- `app/hr/page.tsx`: 회사 선택 버튼을 진한 단색 블록 → 아웃라인 + 텍스트 컬러로 톤다운.
- `app/hr/components/HrSummaryCards.tsx`: 파스텔 통계 카드 4개 → 흰 카드 + 신호색 텍스트(결근=빨강, 나머지는 중립).
- `app/hr/components/EmployeeSearch.tsx` (1,427줄): 연차/병가 상태 박스(초록/빨강/주황), 비용 박스(인디고), 구독 박스(보라) 등 섹션별 파스텔 컬러를 전부 중립화하고, 신호가 실제로 필요한 곳(연체·위험 수치)만 빨강/파랑 유지. 이메일/자산/구독 섹션 제목 이모지 제거 (`lib/i18n.ts`).

**커밋**: `e2de62f`

---

## 6. 출근부 그리드

- `app/hr/[company]/page.tsx`, `app/hr/components/AttendanceGrid.tsx` (합쳐서 2,030줄): 구분선을 3단계 체계로 정리(`--color-line-strong` 신규 토큰 추가) — 그리드 외곽선·헤더·팀 구분줄처럼 진짜 강조가 필요한 곳만 제일 진한 선 사용. 오늘/주말 컬럼 강조를 빨강+하늘색 조합에서 앰버(오늘)+중립 회색(주말)으로 단순화, 입사예정/퇴사자 행 배경색 제거(이미 텍스트 라벨로 표시돼서 중복이었음).
- **휴가코드 범례(L/S/W/T/B/O/C 색칩)는 의도적으로 그대로 유지** — 장식이 아니라 실제로 기능하는 색상 범례라 손대지 않음.
- 메모/지각/조퇴 컨텍스트 메뉴에 남아있던 이모지 제거.

**커밋**: `3582858`

### 트러블슈팅 — PowerShell 인코딩 버그

이 단계에서 `Get-Content -Raw`를 인코딩 지정 없이 써서 UTF-8 파일(한글·특수문자 포함)을 읽었더니 mojibake로 깨지는 사고가 있었음. `git diff`로 손상을 확인하고 즉시 `git checkout`으로 되돌린 뒤, `[System.IO.File]::ReadAllText/WriteAllText`(인코딩 자동 감지)로 다시 처리해서 안전하게 마무리. 이후 모든 대량 치환 작업은 이 방식으로 통일.

---

## 7. 배포

각 단계마다 개별적으로 `git push origin main` + `npx vercel deploy --prod --scope afs-admin-s-projects` 실행, 매번 TypeScript 컴파일 정상 통과 확인 후 `https://hr.afstransco.com`에 반영.

| 단계 | 커밋 |
|---|---|
| Utility Dashboard (Overview/Calendar) + 전체 섹션 | `6685dc3` |
| HR 메인 대시보드 | `e2de62f` |
| 출근부 그리드 | `3582858` |

---

## 남은 범위

- `app/components/Sidebar.tsx` (전역 네비게이션)는 HR/Utility 등 모든 섹션이 공유하는 컴포넌트라 이번 작업에서 의도적으로 제외.
- 그 외 손대지 않은 화면: `/admin`, `/licenses`, `/assets`, `/supplies` 등.

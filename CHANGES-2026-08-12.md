# AFS Admin — Session Log (2026-08-12)

## 개요

Vendor 편집 화면에서 Auto Pay가 대시보드에 반영 안 되던 문제를 파고들다가, 실제로는 세 가지 서로 다른 원인이 겹쳐 있었던 걸 하나씩 걷어낸 세션. 그 김에 US Bank(복사기 리스) 벤더를 신규 등록하고, Cambridge Water 4개월치 결제 이력을 실제 청구 내역과 대조해서 바로잡음. UI 쪽으로는 Utility 섹션에서 HR로 돌아가는 버튼을 추가하고, Overview 페이지 상단 배너를 정리.

---

## 1. Auto Pay가 대시보드에 안 뜨던 문제 — 세 가지 원인

### 1) 마이그레이션 미실행

`supabase/add_service_account_auto_pay.sql` (`utility_service_accounts.is_auto_pay` 컬럼 추가)이 아직 실행 안 된 상태 — 이 상태에서 Vendor 편집 화면의 토글은 저장 시도 자체가 에러로 실패하고 있었음. 사용자가 SQL Editor에서 직접 실행 후 `CLAUDE.md` pending 목록에서 해당 항목 제거.

### 2) 계좌 vs 청구서 데이터 불일치 (기존 데이터)

계좌 레벨 `is_auto_pay`와 실제 청구서(`utility_bills.is_auto_pay`) 값이 따로 놀고 있던 계정들을 서버-role 키로 직접 대조해서 발견:

| 계정 | 계좌 레벨 | 청구서 레벨 | 조치 |
|---|---|---|---|
| ZFS Fontana Water (3개 계좌) | false | true (4건씩) | 계좌 값을 true로 정정 |
| ZFS Burrtec 136689184 | true | false (4건) | 청구서 4건을 true로 반영 |

원인은 `is_auto_pay` 컬럼을 새로 추가할 때 `default false`가 기존 행에도 소급 적용되면서, vendors.yaml 통해 이미 켜져 있던 상태를 덮어썼기 때문.

### 3) 새 계좌 추가 시 청구서에 반영 안 되는 코드 버그

기존에 있던 계좌의 체크박스를 다시 토글할 때는(`toggleAccountAutoPay`) 청구서에도 반영하는 로직이 있었지만, **새 계좌를 처음 추가할 때(`saveAccount`)는 그 로직이 아예 없었음** — AFS Telus가 정확히 이 케이스였음(계좌 자체가 등록된 적이 없었음).

- `app/utilities/vendors/page.tsx`의 두 `saveAccount()` 함수 모두 수정: 저장 성공 후 `acctForm.is_auto_pay`가 켜져 있으면 같은 계좌번호의 기존 청구서에도 `is_auto_pay: true`를 함께 반영, insert 에러도 이제 화면에 노출
- Telus 계좌(6070393200) 신규 등록 + 기존 청구서 19건 Auto Pay 반영 (프로덕션 DB 직접 수정, 사용자 승인 후 진행)

**커밋**: `818e851` Fix Auto Pay not syncing to existing bills when adding a new vendor account

---

## 2. Payment Methods 탭에 Auto Pay 토글 추가

Auto Pay는 서비스 계좌(`utility_service_accounts`) 단위 플래그인데, 결제수단의 "⟳ Auto" 배지(`payment_methods.is_auto`)는 완전히 별개 필드라서 — 결제수단을 링크해도 Auto Pay가 켜지지 않고, 반대로 결제수단 링크 없이도 Auto Pay가 켜져 있을 수 있는 게 혼란스럽다는 지적 (Biscayne Rogers vs Cambridge Rogers 사례).

Vendor 편집 모달의 Payment Methods 탭에 계좌별 Auto Pay 체크박스를 추가 — 결제수단 링크와 같은 화면에서 바로 켜고 끌 수 있고, 기존 `toggleAccountAutoPay`를 그대로 재사용해서 대시보드 동기화·에러 처리가 Accounts 탭과 동일하게 동작.

**커밋**: `f83627b` Surface the Auto Pay toggle in the Vendor edit modal's Payment Methods tab

---

## 3. US Bank(복사기 리스) 벤더 신규 등록

`_inbox` 폴더에 들어온 Ray Morgan Co / US Bank 청구서 5장(4~8월분, ZFS Fontana, 계정 2082868)을 직접 읽어서 등록. `vendors.yaml`에 `us_bank_zfs` 설정은 이미 있었지만 전용 파서는 없는 상태(generic extractor뿐)라, 실제 파이프라인을 돌리는 대신 각 청구서를 직접 읽고 BALANCE FORWARD / CURRENT CHARGES DUE 구조를 하나하나 대조해서 수기 등록.

- Vendor에 서비스 계좌(2082868) 신규 등록
- 청구서 5건 등록 (previous_balance/current_charges 분리, 이번 달 신규 청구액만 중복 없이 카운트되도록)
- 원본 PDF는 `ZFS\US Bank\` 폴더로 정리 + 표준 파일명(`FONTANA_USBank_{발행일}_{청구서번호}.pdf`)으로 변경, Graph API로 OneDrive 공유링크 생성해 각 청구서에 연결

### 후속 정정 (같은 세션, 사용자가 실결제 내역 제공)

- 4월 청구서($506.91): 5월로 Carried Forward
- 5월 청구서($936.52): 최초 이월 로직 중복 적용으로 3배 부풀어 있던 것 발견 → 정정 후 6월로 Carried Forward
- 6월 청구서: 위 5월 오류가 그대로 이월돼 총액이 $4,087.15까지 부풀어 있던 것 원복. 실제 결제 $2,105.35(7/3)에 맞춰 previous_balance를 $905.06으로 정정, 남은 $31.46은 벤더 과실로 확인된 연체료라 면제 처리 → Paid
- 7월 청구서: 사용자가 이미 $398.15(7/30 결제)로 정리해둔 상태 — 잔여 필드(total_due 등)만 일관되게 정리

---

## 4. Cambridge Water(TNT) 4개월 결제 이력 재구성

사용자가 벤더 포털의 실제 거래 내역(5~8월, Adjustment/Late Penalty/Payment 라인 단위)을 제공 — 이를 기준으로 5~8월 청구서 4건의 결제 귀속을 재구성.

**발견한 문제**: 오늘(8/12) 결제한 $225.43을 7월 청구서에 직접 기록했는데, 실제로는 이 금액이 7월분이 아니라 **8월 청구서로 이월된 잔액**(7월에서 넘어온 $222.65 + 8월 신규 연체료 $2.78)을 갚은 것이었음. 7월 청구서 자체의 $158.08은 이미 7/14에 별도로 완납된 상태였고, 같은 금액이 "7월에 남은 잔액"과 "8월로 넘어온 잔액" 두 곳에 동시에 잡혀서 앞뒤가 안 맞았던 것.

재구성 결과 (원 청구서 금액은 그대로, 결제 귀속만 수정):

| 월 | 청구액 | 상태 |
|---|---|---|
| 5월 ($230.37) | 완납 | Paid |
| 6월 ($453.92) | previous balance 완납, 남은 $158.08은 7월로 이월 | Carried Forward |
| 7월 ($380.73) | 6월분 이월액 완납, 남은 $222.65는 8월로 이월 | Carried Forward |
| 8월 ($440.41 = 이월 $225.43 + 신규 $214.98) | 오늘 이월분 완납, 신규분만 남음 | Partially Paid — **잔액 $214.98만 표시** |

계정 전체 잔액 재계산 결과 정확히 $214.98 (기존엔 $440.41로 잘못 표시되던 것 정정). 6월·7월 청구서는 Carried Forward 처리되어 Overdue/Current 목록에서 빠짐.

---

## 5. UI 정리

### Utility 섹션에서 HR로 돌아가는 버튼 (`5c6bd37`... 실제 커밋: `998fc57`)

`/utilities/*`는 자체 `UtilitySidebar`가 메인 Sidebar(HR/Admin 등 네비 포함)를 완전히 대체하는 구조라, 한번 들어가면 돌아갈 방법이 없었음. AFS 로고 옆에 뒤로가기 버튼 추가, HR 접근 권한 있는 사용자에게만 노출(`allowedSections`가 없거나 `'hr'` 포함).

**커밋**: `998fc57` Add a back-to-HR button in the Utility section sidebar

### Utilities Overview 상단 알림 배너 제거

Overdue Bills / Missing Bills / Abnormal Charges 배너 3개 제거 — 같은 정보가 아래 통계 카드·Bill Status 도넛에도 이미 나와서 중복이었음.

**커밋**: `08714bd` Remove the top alert banners from the Utilities Overview page

---

## 배포

모든 코드 변경사항은 `git push` + `npx vercel deploy --prod`로 https://hr.afstransco.com 에 반영 완료. DB 데이터 수정(Auto Pay 정합성, US Bank/Cambridge Water 결제 재구성)은 프로덕션 Supabase에 직접 반영됨(코드 배포와 별개).

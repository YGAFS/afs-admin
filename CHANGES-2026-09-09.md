# Changes — 2026-09-09

## Utility Overview — Monthly Bill Tracking

오버뷰에 청구서 발행일과 업로드 상태를 빠르게 확인할 수 있는 요약 보드를 추가하고, 사용자의 요청에 따라 전체 청구서 목록이 아닌 업체별 월간 패턴 중심으로 정리했습니다.

### 1) 업체별 Monthly Bill Issue Date 보드

- `Bill Issue Date` 이력을 회사·업체·계정번호별로 그룹화
- AFS / TNT / ZFS 컬럼으로 나누어 표시
- 과거 issue date들의 발행일(day of month)을 평균내어 `15th/mo` 형태로 표시
- 괄호 안에 통계에 사용한 청구서 수 표시
- 계정번호가 다른 동일 업체는 별도 행으로 표시
- 업체별 평균 발행일이 지났고 현재 월 issue date 청구서가 없으면 해당 행을 빨간색으로 표시
- 현재 월에 이미 청구서가 업로드되었거나 예상 발행일 전이면 일반 색상 유지
- 31일 발행 패턴은 해당 월의 마지막 날을 기준으로 현재 사이클 여부 판단

### 2) 월별 Utility Bills / SharePoint 링크 박스

- Issue Date 보드 옆에 월별 Utility Bills 박스 추가
- `Due Date` 기준으로 선택한 월의 빌을 조회
- 이전 달 / 다음 달 화살표로 월 이동 가능
- 기본 선택 월은 현재 월
- 업체명, 회사, 선택 월, Due Date 표시
- `Open SharePoint ↗` 버튼을 누르면 저장된 원본 OneDrive/SharePoint 링크를 새 창에서 열 수 있음
- 링크가 없는 청구서는 `No file link`로 표시

### 3) 적용된 커밋 및 배포

| 커밋 | 내용 | 배포 |
|---|---|---|
| `41fae64` | 초기 Invoice upload alert 영역 추가 | 완료 |
| `d3aaa96` | 전체 Bill Issue Date 정렬 보드로 변경 | 완료 |
| `bbec55e` | 회사·업체별 월간 발행일 요약 보드로 변경 | 완료 |
| `808a706` | 현재 청구 사이클 미업로드 행 빨간색 표시 | 완료 |
| `861310b` | 현재 월 Utility Bills 및 SharePoint 링크 박스 추가 | 완료 |
| `550dbd3` | Due Date 기준 월 이동 기능 추가 | 완료 |

현재 프로덕션 주소: <https://hr.afstransco.com>

## Utility Bill Ingestor 점검 및 재가동

2026-09-04에 인박스 감시 상태를 점검한 결과, `Utility Bill Ingestor` 예약 작업은 등록되어 있었지만 Python 감시 프로세스가 실행 중이 아니었고 마지막 실행 결과도 오류 상태였습니다.

### 조치

- Windows Task Scheduler의 `Utility Bill Ingestor` 작업을 수동 시작
- `app.main watch` 감시 프로세스 실행 확인
- OneDrive `_inbox`에 남아 있던 파일 2건 처리 확인
- 인박스가 비워진 것을 확인

### 처리 결과

- `document-0.pdf` → TNT / Enbridge / Cambridge로 정상 분류
- `Rogers-Aug22_2026-3224281858 (1).pdf` → TNT Biscayne Rogers로 인식되었으나 `needs_review` 상태로 분류

감시기는 현재 계속 실행 중이며, 가상환경 실행기와 실제 Python 런타임 프로세스가 함께 보이는 것은 Windows 실행 구조상 정상입니다.

## Utility Email Thread 운영 및 GDE 분리

월별 Utility Bill 등록 알림을 홈페이지와 Outlook 메일 스레드로 함께 관리할 수 있도록 구성했습니다. 이메일은 상세 정보의 원본이 아니라 대시보드 업데이트 알림으로 사용하며, 대시보드 링크는 `https://hr.afstransco.com/utilities/bills`로 연결됩니다.

### 주요 기능

- 월별 최초 이메일 생성 및 전체 Utility Bill 목록 발송
- 빌 등록 월(`created_at`) 기준으로 해당 월의 이메일 스레드 관리
- 개별 Bill 업데이트 알림을 Graph `createReplyAll`로 기존 스레드에 연결
- 최초 메일 재발송 시 기존 스레드에 붙지 않고 완전히 새 메일로 발송
- 재발송 전 경고 문구 표시 및 수동 `Send` / `Resend` 제공
- 자동 발송은 일시 중지하고 Admin 페이지에서 수동 발송하도록 변경
- 관리자 전용 `/utilities/admin` 페이지 추가
- Utility Admin은 `admin@afstransco.com`만 접근 가능
- 이메일 본문에 Utility, Company, Location, 마스킹된 Account, Amount, Due date 표시
- 이메일 본문 중앙 정렬·굵은 글씨·대시보드 버튼·Bill 파일 다운로드 버튼 적용
- 알림 현황에서 해당 월 스레드로 발송된 메일 수와 Bill 이름 목록 확인
- OneDrive/SharePoint 파일 링크 생성 실패 시 재시도하도록 보강

### 수신자 GDE 분리

`UTILITY_EMAIL_RECIPIENT_GROUPS` 환경변수로 수신자 그룹을 여러 개 등록하고 Admin에서 선택할 수 있습니다.

- `TEST GDE` — `testgde@afstransco.com`
- `TEST GDE 2` — `testgde2@afstransco.com`
- `AFS Utility` — `utility@afstransco.com`

각 그룹은 같은 월이라도 별도의 `utility_email_threads` 행과 별도의 Outlook 루트 메일을 사용합니다. 따라서 GDE 1에서 발송한 메일이 GDE 2 스레드에 붙지 않습니다.

### 버그 수정 및 안정화

- GDE 2에 스레드가 없을 때 기본 수신자 그룹으로 잘못 fallback하던 경로 수정
- 발송 후 상태 새로고침을 기다리며 UI가 무한으로 잠기는 현상 수정
- 이전 그룹의 느린 GET 응답이 현재 선택 그룹 상태를 덮어쓰지 않도록 요청 순서 보호
- Admin 상태 조회에 30초 timeout 추가
- Microsoft Graph token/API 요청에 20초 timeout 추가
- 빈 Graph 응답 및 잘못된 JSON 응답을 명확한 오류로 처리
- draft message ID가 반환되지 않을 때 발송을 진행하지 않도록 검증

### 관련 DB 변경

`utility_email_threads`에 `recipient_group_id`를 추가하고 다음 조합을 unique 처리했습니다.

`billing_month + sender_email + recipient_group_id`

기존 `utility_email_notifications`에는 발송 상태, 재시도 횟수, 오류, Graph message ID, 발송 시각을 기록합니다.

### 적용 커밋 및 배포

| 커밋 | 내용 | 배포 |
|---|---|---|
| `8b7a07b` | 수동 Utility Bill 이메일 컨트롤 복구 | 완료 |
| `d833eb6` | 이메일 포맷 및 대시보드 링크 개선 | 완료 |
| `d8280c0` | 전용 ingestor 이메일 secret 사용 | 완료 |
| `3b62c28` | 등록 월 기준 이메일 스레드 적용 | 완료 |
| `15196d9` | 이메일 알림에 Bill 식별 정보 추가 | 완료 |
| `15c04cb` | 계정번호 등 긴 정보 축약 | 완료 |
| `690165c` | 수신자 그룹 선택 및 수동 발송 추가 | 완료 |
| `24e1c4b` | Utility 사용자 접근 권한 및 undefined 라우팅 수정 | 완료 |
| `c143c54` | OneDrive 링크 생성 재시도 | 완료 |
| `55eafdf` | 수신자 그룹별 이메일 스레드 분리 | 완료 |
| `982fcce` | 그룹 간 스레드 교차 연결 방지 | 완료 |
| `dfdbd5a` | GDE 2 라우팅 및 발송 프리즈 수정 | 완료 |

현재 프로덕션 주소: <https://hr.afstransco.com>

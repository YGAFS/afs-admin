# AFS Admin 변경 내역 — 2026-09-11

현재까지 진행한 Utility Bill 관리 및 자동 수집 기능의 변경·복구·배포 내역입니다.

## Utility Bill 화면 개선

- Utility Bill 목록과 월별 유틸리티 목록에 내부 스크롤을 적용해 화면이 무한히 길어지지 않도록 개선
- 알림 팝업은 한 번에 3개씩 보이고 내부 스크롤로 읽지 않은 전체 알림을 확인할 수 있도록 개선
- 알림 팝업 바깥 영역을 클릭하면 자동으로 닫히도록 변경
- 동일 Bill에 대해 `New bill`과 `Bill updated` 알림이 중복 생성되지 않도록 정리
- 결제 상태 변경 알림은 제외하고 새 Bill 등록 알림만 생성하도록 변경
- Bill 목록에 업로드일과 이메일 발행일을 구분해 표시
- 알림 및 Bill 목록의 중복 표시를 줄이고 최신 내역 중심으로 정리

## Utility Email 발송

- 여러 Bill을 체크박스로 선택해 하나의 이메일 안에 표 형태로 묶어 발송
- 같은 회사·같은 공급자의 여러 Bill을 최초 월간 메일과 같은 형태로 병합
- 재발송을 허용하고 마지막 발송 시각을 표시
- Bill 정보 아래, Dashboard 버튼 위에 사용자 메시지 입력란 배치
- 입력한 메시지를 Bill 요약 이메일에 함께 포함
- 다운로드 버튼을 각 Bill 정보의 우측으로 배치
- 이메일 본문에 Utility, Company, Location, Account, Amount, Due date 표시
- 파일 다운로드 링크와 Utility Dashboard 링크 제공
- 수신자 그룹별 월간 이메일 스레드 분리 및 수동 발송 관리

## Python Utility Bill Ingestor

- Windows 재부팅 이후에도 Bill inbox watcher가 자동 실행되도록 Task Scheduler/Watchdog 구성
- `needs_review` 파일을 다시 inbox에 넣으면 재처리할 수 있도록 복구 흐름 개선
- 동일 파일 해시 중복 등록 방지
- 계정번호 비교 시 공백과 하이픈을 무시하도록 정규화
  - 예: `5-0781-2423`와 `507812423`을 같은 계정으로 인식
- 서비스 계정과 Bill을 연결하고 Auto Pay 설정을 신규 Bill에 반영
- Vendor 화면에서 Auto Pay를 변경하면 기존 매칭 Bill도 함께 갱신

### Vendor별 처리

- Rogers 청구서의 `Adjustments` 크레딧을 읽어 총액에 반영
- AFS Rogers 기존 Bill 7건을 서비스 계정과 연결하고 Auto Pay 상태를 백필
- Orkin 청구서에서 서비스일 다음의 invoice 번호를 추출
- Orkin은 같은 계정·같은 월·같은 금액이어도 서비스일/invoice 번호가 다르면 별도 Bill로 등록
- 잘못 중복 분류된 Orkin 청구서 복구
  - `2026-08-21 / Invoice 301056615` 정상 등록
  - 기존 `2026-08-31 / Invoice 301056616`도 invoice 번호 보완

## 데이터 복구 및 정리

- 중복으로 분류된 유효 Orkin PDF를 `_review\duplicates`에서 `_inbox`로 복구
- 잘못된 import 기록을 재처리 가능 상태로 되돌린 뒤 정상 Bill로 등록
- Orkin 두 서비스일 청구서가 각각 별도 내역으로 표시되는 것을 확인
- Rogers 계정번호 표기 불일치로 연결되지 않던 기존 Bill의 `service_account_id`와 Auto Pay 상태 보완

## 검증

- Orkin extractor, 계정번호 정규화, classifier 관련 테스트 23개 통과
- Auto Pay 계정 매칭 관련 테스트 26개 통과
- Next.js production build 성공

## 배포 기록

| 커밋 | 내용 | 상태 |
|---|---|---|
| `6ddf531` | Rogers Bill 크레딧 추출 | 배포 완료 |
| `1ed2e93` | 계정번호 정규화 및 Auto Pay 서비스 계정 매칭 | 배포 완료 |
| `fab9085` | Orkin invoice 번호 기반 중복 감지 | 배포 완료 |

Production: <https://hr.afstransco.com>

## 운영 참고

- Python ingestor는 별도 Windows 프로세스로 실행되며, 코드 변경 후에는 기존 watcher를 재시작해야 새 extractor가 적용됩니다.
- 동일 watcher가 여러 개 실행되면 한 파일을 서로 다른 프로세스가 먼저 처리할 수 있으므로 Task Scheduler 작업은 하나만 활성화하는 것이 안전합니다.

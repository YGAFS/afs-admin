# AFS Admin — Session Log (2026-08-11 ~ 2026-08-13)

## 개요

Utility Bill Ingestor에 Microsoft Graph 기반 OneDrive 파일 링크를 새로 붙이는 작업으로 시작해서, 그 과정에서 여러 벤더(Cambridge Water, Burrtec, Rogers)의 실제 청구서 원본을 다시 열어보며 발견한 추출기 버그와 실데이터 오류를 정리하고, Vendor 관리 화면(결제수단 연결, 계좌별 Auto Pay)과 관리자 계정 권한 패널을 새로 추가한 세션입니다.

커밋 다수, `tools/utility_bill_ingestor/` (Python) + `app/` (Next.js) 양쪽에 걸쳐 있음. 코드 변경 없이 Supabase 데이터만 직접 수정한 작업도 다수 포함(아래 각 섹션에 표시).

---

## 1. Microsoft Graph API — OneDrive 파일 링크 연동

### 커밋: `98bcb1e`

Ingestor가 청구서를 등록할 때마다 조직 범위(organization-scoped) OneDrive 공유 링크를 생성해 `utility_bills.onedrive_file_url`에 저장 — 웹앱은 이미 이 필드가 있으면 📎 "Open file" 링크를 그려주므로 사이트 코드 변경 없이 바로 작동.

- `app/graph_client.py`: app-only client-credentials 방식 Graph 클라이언트 (HR 메일 발송용 `lib/msal.ts`의 delegated 방식과는 별개 앱 등록)
- `scripts/graph_discover_drive.py`: Tenant ID/Client ID/Secret만 있을 때 대상 드라이브(SharePoint site) 찾는 1회성 스크립트
- `scripts/backfill_onedrive_links.py`: 기존 청구서 일괄 백필용
- 실제 Azure 앱 등록 진행 중 발견한 이슈:
  - 처음엔 `Files.Read.All`(읽기 전용)로 진행했으나 `createLink`는 쓰기 작업이라 `Files.ReadWrite.All`로 재승인 필요했음
  - OneDrive 동기화 클라이언트가 SharePoint 폴더 shortcut을 로컬에서 다른 이름으로 동기화하는 경우가 있어(`afstrans.co - AFS_2023` ↔ 실제 드라이브상 `AFS_2023`), `GRAPH_DRIVE_ROOT_REMOTE_PREFIX` 설정으로 로컬 경로와 실제 드라이브 경로를 분리해서 매핑
- 기존 청구서 60건 전부 백필 완료, 이후 신규 처리분 4건 추가 백필

---

## 2. Cambridge Water(TNT) 추출기 버그 — 수개월간 검토행 원인

### 커밋: `98bcb1e`, `bb22d19`

PDF에 `Current Water/Wastewater Charges` 라벨이 **한 장에 두 번** 찍히는데, 첫 번째(요약 박스 직후)는 실제로 "이번 달 청구액"이 아니라 "총 청구액"이 뒤따름 — 추출기가 계속 이 첫 번째(잘못된) 값을 읽어서 `previous + current ≠ total`로 매번 needs_review로 빠지고 있었음. 두 번째(Total Charges 옆) 값이 진짜 이번 달 청구액.

실물 PDF 5~8월분 전부 대조 후 추출기 수정 + 회귀 테스트 추가. 5~8월 청구서 재처리:

| 월 | Balance Forward | 이번달 청구액 | Total Due |
|---|---|---|---|
| 5월 | $26.77 | $203.60 | $230.37 |
| 6월 | $295.84 | $158.08 | $453.92 |
| 7월 | $160.06 | $220.67 | $380.73 |
| 8월 | $225.43 | $214.98 | $440.41 |

**추가로 발견한 폴더/OneDrive 버그** (`bb22d19`): 이 벤더만 새로 만든 "Cambridge" 하위폴더에 파일을 넣으면 OneDrive 공유 링크가 반복적으로 안정적으로 안 붙는 현상 확인 → `flat_archive: true` 설정 추가해서 이 벤더만 하위폴더 없이 기존 평평한 `TNT\Cambridge Water\` 구조 그대로 유지하도록 변경.

### 실데이터 정산 (코드 변경 없음, 사용자가 직접 결제 내역 제공)

5~8월 실제 결제 히스토리(부분 결제가 다음 달로 이월되는 구조)를 하나하나 대조해서 반영:
- 5월: Paid (Late), $233.25 (원금+연체료)
- 6월: Carried Forward — 6/10 결제($295.84)가 6월 Balance Forward를 정확히 정리, 남은 $158.08은 7월 Previous Balance로 자연 이월된 것이라 별도 연체로 중복 경고하지 않도록 처리
- 7월: Overdue (Partial), 잔액 $222.65 (7/14 결제 $158.08 반영)
- 8월: 미납 진행 중 ($440.41, 기한 8/28)

---

## 3. review 폴더를 OneDrive로 이동

### 커밋: `98bcb1e`

`processing/`·`failed/`·`logs/`는 로컬 유지(동기화 레이스 방지)하되, `review/`는 완료된 이동으로만 채워지는 폴더라 레이스 위험이 없어 `UTILITY_BILL_REVIEW_DIR` 설정으로 OneDrive의 `_inbox` 옆 `_review` 폴더로 이동 — 어느 기기에서든 검토 필요한 파일을 바로 확인 가능.

---

## 4. 대시보드 UI 정리

### 커밋: `41438a3`

- 작동 안 하던 죽은 버튼 3개(Download Report / 알림 / Add Utility Bill) 삭제
- 왼쪽 유틸리티 사이드바 글자 크기 한 단계 확대
- Current 탭에서 `is_auto_pay`가 켜진 청구서는 클릭 가능한 초록 "✓ Paid" 버튼 대신 파란 "⟳ Auto Pay" 배지로 표시(실수로 눌러 상태가 꼬이는 것 방지)

---

## 5. Vendor 결제수단 — 새로 등록 대신 기존 것 연결

### 커밋: `4cc1a4c`

`payment_methods`는 원래 회사 단위로만 공유되던 목록이라 벤더별 재사용 개념이 없었음. `vendor_payment_methods` 조인 테이블(마이그레이션 `add_vendor_payment_methods.sql`) 추가해서:
- Payment Methods 탭에 이제 **이 벤더에 연결된 것만** 표시
- 드롭다운으로 같은 회사 내 기존 결제수단을 골라 "Link"만 하면 연결 (매번 새로 입력할 필요 없음)
- "✕"는 이제 결제수단 자체 삭제가 아니라 이 벤더에서만 연결 해제(다른 벤더가 같은 수단을 쓰고 있어도 안전)

---

## 6. Vendor 계좌별 Auto Pay 토글 + 관리자 사용자 접근 패널

### 커밋: `5c6bd37`, `d53cd48`

- `utility_service_accounts.is_auto_pay` 컬럼 추가(`add_service_account_auto_pay.sql`) — Vendor 편집 화면 Accounts 탭에서 계좌별로 직접 켜고 끌 수 있음. Ingestor도 이 값을 읽어서(`vendors.yaml`의 벤더 단위 `auto_pay` 플래그와 별개로, 계좌 단위로) 새 청구서에 반영
- `/admin` Settings 페이지 하단에 **User Access 패널** 신설 (`admin@afstransco.com` 전용) — Supabase 로그인 계정 목록을 불러와 계정별로 Full Access 또는 섹션별 체크박스로 노출 범위 조절. 로그인 계정 목록을 가져오려면 Auth Admin API(service-role)가 필요해서 이 저장소 최초의 Next.js API 라우트(`app/api/admin/users/route.ts`)를 추가, 서버 전용 `SUPABASE_SERVICE_ROLE_KEY`는 `.env.local`과 Vercel Production 환경변수에만 설정(브라우저에는 절대 노출 안 됨)

---

## 7. Bill History by Vendor — 연도별 접기

### 커밋: `3666ecb`

벤더를 펼치면 청구서가 16개월 넘게 flat하게 쏟아지는 문제 → 연도별로 묶고, **올해만 기본 펼침**·이전 연도는 접힘으로 시작. 연도 헤더 클릭으로 개별 펼치고 접기 가능(여러 연도 동시에 펼쳐도 됨).

---

## 8. Auto Pay 설정 정정 — TNT Cambridge Water가 아니라 ZFS Fontana Water

### 커밋: `862617a`

처음에 "Water는 자동이체"라는 요청을 TNT Cambridge Water로 잘못 적용했다가, 사용자 확인 후 정정: TNT Cambridge Water 4건은 Auto Pay 해제, ZFS Fontana Water 전체 12건에 Auto Pay 적용. `vendors.yaml`의 `auto_pay` 플래그도 같은 실수 방지 위해 Fontana Water 쪽으로 이동.

### 실데이터 정리 (코드 변경 없음)

Fontana Water 계좌 106310/106311의 6월분이 두 개씩 있어 "중복"처럼 보였던 문제 — 실제로는 중복이 아니라 `billing_month`가 issue_date가 아닌 잘못된 기준으로 계산되어 4월분이 5월로, 5월분이 6월로 밀려 진짜 6월분과 겹쳐 보인 것. 4건의 `billing_month`를 issue_date 기준으로 재계산해서 4~7월로 깔끔하게 정리(삭제 없이 라벨만 수정).

---

## 9. Burrtec 이중 계좌(136675222 / 136689184) 정리

### 커밋: `f8fde64`, `bb22d19`(같은 커밋에 flat_archive 포함), `01b6b0a`

**근본 원인 — 감시 프로세스 중복 실행**: Task Scheduler 재시작 시 이전 프로세스가 완전히 안 죽고 새 프로세스와 동시에 떠 있던 것 발견 → 같은 파일을 두 프로세스가 동시에 집어서 3월 5222 청구서가 중복 등록됨. 프로세스 정리 후 항상 하나만 떠 있는지 확인하는 절차 확립.

**추출기 버그**: `Total Previous Balance`/`Current Charges`/`Total Amount Due` 구조가 있는데, 결제·크레딧이 낀 달은 이번 청구액이 마이너스로 나오거나 수량 필드("2.38톤" 등 소수점 2자리라 금액처럼 보임)까지 합산되는 문제 발견 → 실제 라인아이템(날짜/수량/설명/금액 4줄 구조)을 정확히 파싱하도록 재작성, 회귀 테스트 추가.

**계좌 136675222 실데이터**: 3~5월 청구서가 매달 정확히 $717.91로 동일해서 "매달 새로 발생"처럼 보였지만, 실제로는 3월 이후 결제가 한 번도 없어서 같은 미납액이 계속 재청구되던 것("Last Payment Received on 03/15/26" 문구가 3~5월 청구서 모두 동일). 3월만 실제 발생액으로 남기고 4·5월은 Carried Forward 처리해서 3중 계산 방지.

**Auto Pay**: 사용자 확인 후 두 계좌 모두 Auto Pay 적용, `vendors.yaml`을 계좌별로 분리(`burrtec_zfs_5222` / `burrtec_zfs_9184`)해서 계좌마다 다른 Auto Pay 여부를 관리할 수 있도록 변경.

---

## 10. TNT Biscayne Rogers 추가 — 계약명 교차(AFS 명의, TNT 실사용)

### 커밋: `9e77d60`, `0fd9eed`, `166c4c9`

TNT Biscayne 지점의 Rogers 5G 라우터 와이파이 청구서를 처음 넣었을 때 AFS/Surrey로 잘못 등록됨 — 원인은 버그가 아니라 **이 계좌의 Rogers 계약 자체가 AFS 명의**("ACME FREIGHT SALES LIMITED", Surrey 청구지)로 되어 있어서, 청구서 본문에 "Biscayne"이나 "TNT"라는 단어가 전혀 없고 항상 "Surrey/AFS"만 찍혀 있기 때문. 텍스트 기반 자동 분류로는 절대 구분 불가 → 계좌번호(`5-0882-6140`)를 `rogers_tnt_biscayne` 설정에 직접 등록해서, 텍스트 내용과 무관하게 계좌번호만으로 항상 TNT Biscayne으로 분류되도록 처리. 기존에 잘못 등록된 청구서도 TNT/Biscayne으로 재분류, 파일도 `AFS\Rogers\` → `TNT\Rogers\Biscayne\`로 이동.

### Vendor 목록 분리 (`166c4c9`)

같은 "Rogers"라는 이름으로 Cambridge·Biscayne 두 계좌가 하나의 벤더 행에 묶여 Vendor 목록에서 하나로 합쳐져 보이던 문제 → `utility_vendors`를 "Cambridge Rogers" / "Biscayne Rogers" 두 개 행으로 분리(기존 Cambridge 청구서 7건의 `provider` 필드도 함께 변경), `vendors.yaml`도 대응하는 이름으로 맞춤. 파일 저장 위치는 기존 `TNT\Rogers\...` 폴더 구조 그대로 유지.

---

## 11. Payment Methods / Auto Pay 저장이 "조용히 실패"하던 버그

### 커밋: `08176ad`, `1616054`

- **Payment Methods Link 안 됨**: `vendor_payment_methods` 마이그레이션이 아직 실행 안 된 상태였음 — 테이블이 없어서 저장 시도가 에러 없이 조용히 실패. 마이그레이션 실행 후 정상 작동 확인.
- **Auto Pay가 대시보드에 반영 안 됨**: 계좌 단위 Auto Pay 토글이 `utility_service_accounts`에만 저장되고 있었는데, 대시보드의 Auto Pay 배지는 각 **청구서(bill)** 행의 값을 보고 표시 — 그래서 토글을 켜도 이미 등록된 청구서에는 반영이 안 되고 있었음. 토글 시 같은 계좌번호의 기존 청구서 전부에도 `is_auto_pay`를 함께 반영하도록 수정.
- **더 근본적인 버그**: 두 토글 함수 모두 저장 요청의 에러를 전혀 확인하지 않았음 — 특히 Vendor 편집 모달 쪽은 저장 실패해도 화면에 낙관적으로 "켜짐"으로 먼저 표시해버려서, 실제로는 DB에 반영 안 됐는데도 저장된 것처럼 보이는 상태가 가능했음(Biscayne Rogers가 정확히 이 케이스). 이제 실패 시 체크박스가 원래대로 되돌아가고 에러 메시지가 화면에 뜨도록 수정.

---

## 미해결 / 참고사항

- 오래된 AFS/Rogers 위치에 있던 원본 PDF 1개가 OneDrive 동기화 중 잠겨 있어서 삭제가 아직 안 됨(새 위치엔 복사본 정상 존재, 기능상 문제 없음) — 동기화 끝나면 정리 필요
- Ingestor 감시 프로세스를 재시작할 때마다 이전 프로세스가 완전히 종료됐는지 반드시 확인할 것(9번 섹션의 Burrtec 중복 등록 원인)
- TNT `9184` 계좌는 2026-08-15부터 Auto Pay 전환 예정이라고 사용자가 안내함 — 전환 확인되면 `vendors.yaml`의 `burrtec_zfs_9184` 항목에 `auto_pay: true` 반영 필요

# AFS Admin — Session Log (2026-07-21)

## 개요

Utility Management Phase 2 UI 구현 완료 및 Phase 3 분석/알림 기능 구현.

---

## Phase 2: Vendors 상세 패널 + Locations 관리

### 커밋: `7208903`

#### 구현 파일

| 파일 | 내용 |
|------|------|
| `app/utilities/vendors/page.tsx` | 전면 재작성 |
| `app/utilities/locations/page.tsx` | 전면 재작성 |
| `app/utilities/components/UtilityShell.tsx` | Locations 탭 추가 |
| `supabase/phase2_utility_management.sql` | 신규 테이블 마이그레이션 (완료) |

#### Vendors 페이지 (`/utilities/vendors`)

- 테이블 뷰: 회사 / Location 필터 + 키워드 검색
- **행 클릭 → 오른쪽 Detail Panel** 슬라이드인
  - **Contacts**: 이메일(mailto), 전화(tel), 클릭 복사, Primary 배지, 인라인 추가/삭제
  - **Service Accounts**: 계정번호 클릭 복사, 위치 표시, Account Portal 링크, 인라인 추가/삭제
  - **Contract Period** 표시
  - **Documents**: OneDrive 링크 열기, 문서 타입 아이콘, 인라인 추가/삭제
- **Website / Billing Portal** 버튼
- Add/Edit 모달: 회사, 이름, 서비스 타입, 웹사이트, Billing Portal, 계약 기간, 메모

#### Locations 페이지 (`/utilities/locations`)

- 회사별 카드 그리드 (AFS/TNT/ZFS 색상 구분)
- 카드: 도시/지역, 주소, **Vendor 수 + Bill 수** 통계
- 호버 시 Edit / Delete 노출
- Add / Edit 모달

#### UtilityShell 내부 탭 순서

```
Overview → Locations → Vendors → Bills
```

#### 수동 실행 완료 (Supabase SQL Editor)

```sql
-- supabase/phase2_utility_management.sql
-- utility_locations, utility_vendor_contacts,
-- utility_service_accounts, utility_document_links 테이블 생성
-- ALTER utility_vendors ADD website_url, billing_portal_url
-- ALTER utility_bills ADD location_id
```

---

## Phase 3: 비용 분석 + 알림

### 커밋: `f908e91` + `3935a99` (타입 픽스)

#### 구현 파일

| 파일 | 내용 |
|------|------|
| `app/utilities/overview/page.tsx` | 전면 개선 |
| `app/utility/page.tsx` | 모달/테이블 개선 |

#### DB 컬럼 추가 (수동 실행 완료)

```sql
ALTER TABLE utility_bills
  ADD COLUMN IF NOT EXISTS previous_balance numeric(10,2) DEFAULT 0,
  ADD COLUMN IF NOT EXISTS current_charges  numeric(10,2),
  ADD COLUMN IF NOT EXISTS billing_year     int,
  ADD COLUMN IF NOT EXISTS service_account_id uuid
    REFERENCES utility_service_accounts(id) ON DELETE SET NULL;

UPDATE utility_bills
SET billing_year = EXTRACT(YEAR FROM due_date)::int
WHERE billing_year IS NULL AND due_date IS NOT NULL;
```

#### Overview 페이지 개선

- **Previous Balance / Current Charges 분리**
  - `current_charges` 기준으로 Monthly Spend KPI 계산 (이전 잔액 제외)
  - `amount` = previous_balance + current_charges (Total Due)
- **KPI 카드 (5개)**
  - Monthly Charges (Current) — previous_balance 제외
  - 3-Month Avg (Charges) — trailing 3개월 평균
  - Overdue Total Due
  - Due This Week
  - Paid This Month
- **차트**
  - 6M / 12M 토글 가능한 막대 차트 (Monthly Charges Trend)
  - Bill Status Donut (Paid / Upcoming / Overdue / Missing / Abnormal 카운트)
- **Abnormal Charge 감지**
  - 같은 provider + account_number 기준 직전 3개월 평균 대비 ±25% 초과 시 플래그
  - 최소 2개 이상 비교 데이터 필요
- **Missing Bill 감지**
  - 지난달에 청구서가 있었으나 이번달 없는 provider/account 감지
  - 매월 5일 이후부터 플래그 (청구서 도착 유예 기간)
- **알림 바** (페이지 상단): Overdue / Missing / Abnormal 요약
- **Vendor별 지출** 바 차트 (all-time current_charges 기준)
- **Location별 지출** 바 차트 (location_id 연결된 bill 기준)
- **Abnormal + Missing** 전용 패널 (3열 마지막)

#### Bills 페이지 (`/utility` → `/utilities/bills`) 개선

- **모달 입력 확장**
  - Previous Balance 필드
  - Current Charges 필드
  - Total Due: 두 값이 입력되면 자동 계산 표시 (비활성화)
  - 저장 시 `billing_year` 자동 세팅 (issue_date 우선, 없으면 due_date)
- **테이블 Amount 셀**
  - `current_charges` 있으면 `Total Due` + `prev / curr` 분리 표시
  - 없으면 기존 `amount` 표시
- **Abnormal 배지** (`±25%`): utility_name 옆 인라인 표시

---

## Codex 연동 (PDF 청구서 → SQL 추출 가이드)

PDF 청구서에서 `previous_balance`, `current_charges`, `billing_year` 등을 추출해 SQL UPDATE 문 생성하는 프롬프트를 작성함. ChatGPT o3 / Claude 등 PDF 읽는 모델에 청구서 첨부 후 실행.

출력된 UPDATE SQL → Supabase SQL Editor에서 실행.

---

## 배포 이력

| 커밋 | 배포 URL | 내용 |
|------|----------|------|
| `7208903` | `hr.afstransco.com` | Phase 2 UI 완료 |
| `f908e91` | 실패 (TS 오류) | Phase 3 |
| `3935a99` | `hr.afstransco.com` | Phase 3 타입 픽스 후 성공 |

---

## 현재 DB 스키마 (utility 관련)

```
utility_bills
  id, company_id, utility_name, provider, amount, currency
  previous_balance, current_charges          ← Phase 3 추가
  billing_year, billing_month, billing_period
  issue_date, due_date, bill_number, account_number
  is_auto_pay, payment_method_id
  onedrive_file_url, is_paid, paid_at, paid_by
  location_id                                ← Phase 2 추가
  service_account_id                         ← Phase 3 추가 (미사용)
  notes, created_at, updated_at

utility_vendors
  id, company_id, name, service_type
  contact_name, contact_email, contact_phone (legacy)
  website_url, billing_portal_url            ← Phase 2 추가
  contract_start, contract_end, onedrive_url (legacy)
  notes, created_at, updated_at

utility_locations            ← Phase 2 신규
  id, company_id, region, city, name, address, notes

utility_vendor_contacts      ← Phase 2 신규
  id, vendor_id, name, title, email, phone, is_primary, notes

utility_service_accounts     ← Phase 2 신규
  id, vendor_id, location_id, account_number
  service_label, billing_portal_url, is_active, notes

utility_document_links       ← Phase 2 신규
  id, vendor_id, location_id, name, document_type, onedrive_url

payment_methods
  id, company_id, label, holder_name, card_brand, bank_name, is_auto, notes

utility_user_roles
  id, user_id, email, role ('admin'|'ap'), notify_email
```

---

## 라우팅 구조

```
/utilities              → redirect → /utilities/overview
/utilities/overview     ← Phase 1+3 (대시보드)
/utilities/locations    ← Phase 2 (카드 그리드)
/utilities/vendors      ← Phase 2 (테이블 + 상세 패널)
/utilities/bills        ← re-export from /utility (Phase 3 개선)
/utility                ← 원본 Bills 페이지 (redirect 대상)
```

---

## 남은 잠재적 작업

- [ ] `service_account_id` FK를 bills에 실제 연결 (현재 컬럼만 추가, UI 미구현)
- [ ] Bill PDF backfill: Codex로 `previous_balance` / `current_charges` 추출 후 UPDATE
- [ ] Location별 필터를 Bills 테이블에 적용 (현재 Overview만)
- [ ] Missing bill 알림 이메일 발송 (현재 UI 표시만)

# AFS Admin — Session Log (2026-08-06 ~ 2026-08-07)

## 개요

Utility Vendor 관리 화면 소규모 개선 4건 + **Utility Bill PDF 자동 처리 도구(신규)** 구축·검증·실데이터 정리까지 진행한 세션. 커밋 6개, `tools/utility_bill_ingestor/` 신규 Python 워커 추가, Supabase `utility_bills`/`utility_bill_imports`에 대한 대규모 수동 데이터 정리 포함.

---

## 1. Vendor 편집 모달 — Location 버튼 선택

### 커밋: `d0ece2f`

Company 선택과 동일하게 Location도 드롭다운이 아닌 버튼 방식으로 변경. 선택된 Company에 속한 Location만 표시.

| 파일 | 내용 |
|------|------|
| `app/utilities/vendors/page.tsx` | `Vendor.location_id` 필드 추가, Edit/Add Vendor 모달에 버튼형 Location 선택기 추가, Company 변경 시 소속 안 맞는 Location 자동 해제 |
| `supabase/add_vendor_location.sql` | `utility_vendors.location_id` 컬럼 추가 (실행 완료) |

---

## 2. Vendor 모달 — Accounts 탭 + Location 계약순 정렬

### 커밋: `67df6fc`

| 파일 | 내용 |
|------|------|
| `app/utilities/vendors/page.tsx` | Edit Vendor 모달에 "Details"/"Accounts" 탭 추가 — 계정번호 여러 개 등록/삭제 가능 (기존 `utility_service_accounts` 테이블 재사용, 신규 마이그레이션 불필요). `utility_locations.sort_order` 기반 정렬 로직 추가 (컬럼 없어도 안전하게 이름순 폴백) |
| `supabase/add_location_sort_order.sql` | `sort_order` 컬럼 추가 + TNT 계약순(Cambridge→Biscayne→Pickering) 값 채움 (실행 완료) |

---

## 3. Vendor 상세 패널 — 평균 발행일/마감일 표시

### 커밋: `bbb24bd`

기존 업로드된 청구서 기준으로 벤더별 평균 발행일/마감일을 "몇 일" 단위로 계산해 상세 패널에 표시 (`Avg Invoice Date: 17th · Avg Due Date: 16th`). `issue_date` 컬럼이 이미 존재해 별도 마이그레이션 불필요.

---

## 4. 사용자별 섹션 접근 제어

### 커밋: `73bf29f`

특정 로그인 계정을 Utility Dashboard 등 특정 섹션만 보이게 제한하는 기능. YG(`yungyeong.j@afstransco.com`) 계정을 Utility 전용으로 설정하기 위해 구현.

| 파일 | 내용 |
|------|------|
| `app/providers.tsx` | 로그인한 사용자 이메일로 `user_access` 조회, `allowedSections` context 제공 |
| `app/components/Sidebar.tsx` | `allowedSections`에 없는 상단 메뉴 항목 숨김 |
| `app/components/ConditionalLayout.tsx` | 허용 안 된 섹션 경로 접근 시 첫 번째 허용 섹션으로 리다이렉트 |
| `supabase/add_user_access.sql` | `user_access` 테이블 생성 + YG 행 삽입 (실행 완료) |

**주의**: 클라이언트 레벨 제한만 구현됨 (RLS는 대부분 테이블에서 `using (true)`로 열려있어 API 직접 호출 시 우회 가능 — 내부 신뢰 직원용 UI 제한 수준).

**미완료**: `yungyeong.j@afstransco.com` Supabase Auth 로그인 계정 자체는 대시보드에서 수동 생성 필요.

---

## 5. Utility Bill PDF 자동 처리 도구 (신규, 대규모)

### 커밋: `de3f2d5`, `d76faf4`

`tools/utility_bill_ingestor/` — 다운로드한 유틸리티 청구서 PDF를 이름 변경 없이 `_inbox` 폴더에 넣기만 하면 자동으로 벤더/사업장/계정/금액/날짜를 추출해 Supabase에 등록하고, 표준 파일명으로 정리해서 기존 `{COMPANY}\{VENDOR}\...` 폴더 구조로 옮기는 별도 로컬 Python 워커.

### 구조

| 파일 | 역할 |
|------|------|
| `app/main.py` | CLI (`watch`, `process-all`, `process-file`, `dry-run`) |
| `app/watcher.py` | Inbox 폴링, 파일 안정성 확인 후 `processing/`로 원자적 이동, 크래시 복구 |
| `app/pipeline.py` | 해시→추출→분류→검증→upsert→파일명 생성→아카이빙→로그 전체 흐름 |
| `app/extractors/*.py` | 벤더별 PDF 파서 (Telus, Rogers, Enbridge, Grandbridge, Cambridge Water, Fontana Water, Burrtec, Orkin, + generic 폴백) |
| `app/classifier.py` / `validator.py` / `filename_builder.py` / `repository.py` | 벤더·사업장 판별 / 보수적 검증(애매하면 무조건 review) / 표준 파일명 생성 / Supabase 읽기·쓰기 (UUID 해석 포함) |
| `tests/` | 63개 단위·통합 테스트, 실제 Supabase 연결 없이 동작 |

### DB

`supabase/add_utility_bill_imports.sql` — 처리 이력/중복방지 감사 로그 테이블 (실행 완료).

### 검증 및 실데이터 정리 (코드 배포와 별개로 진행한 수동 작업)

1. 실제 `0. DB` 폴더의 청구서 62개를 대상으로 dry-run → 실제 처리 진행.
2. 처리 중 **버그 5개 발견 및 수정**: 날짜 정규식이 세금등록번호를 날짜로 오인식(Rogers), 콜론 없는 라벨 레이아웃 미대응(Cambridge Water), 계정번호 하드코딩(Cambridge Water), 라벨/값이 다른 줄에 있는 레이아웃 미대응(Grandbridge, Burrtec), dry-run이 실제로 원본 파일을 이동시키던 안전성 버그.
3. **DB 중복 삽입 사고 발생 및 정정**: 예전 AI-SQL 방식으로 입력된 기존 데이터가 `billing_month`를 발행월이 아닌 다른 기준(파일명 등)으로 저장해둬서, 신규 파이프라인의 중복 체크(billing_year/month 기준)가 이를 못 찾고 총 13건을 중복 삽입함. 발행일 기준 재검증으로 전량 발견·삭제하고 기존 행에 올바르게 연결함. **최종적으로 신규 중복 0건 확인.**
4. Cambridge Water 2건은 "청구서 전체 금액과 기존에 기록된 부분결제 금액"의 관계로 판단, `amount_paid`/`remaining_balance`/`balance_status='partially_paid'`로 재정리.
5. Fontana Water 6건 + Orkin 6건은 계정번호까지 정확히 맞춰보니 기존 DB 값과 실제로 일치(예전 방식이 "이번 기간 신규 청구액만" 추적했던 것으로 확인) — 안전하게 연결.
6. Rogers(TNT) 5건 중 4건은 날짜 버그 수정 후 기존 값과 일치 확인해 연결, 1건(5월/6월)은 완전히 동일한 인보이스가 두 파일로 존재해 하나는 등록·하나는 `duplicate`로 명시 처리.
7. **재시도 버그 발견·수정**: `needs_review`로 분류된 파일을 review 폴더에서 다시 inbox에 넣고 재처리하면 `utility_bill_imports.source_file_hash` 유니크 제약 위반으로 크래시하던 버그. `completed`/`duplicate`만 최종 상태로 취급하도록 수정하고, import 로그를 해시 기준 upsert로 변경.

### 결과

- review 대기 46건 → **3건**(Burrtec 계정 136689184, 연체 이력 있어 안전한 재조정 공식을 못 찾아 수동 확인 필요 상태로 남김)
- 실제 신규 청구서(2026년 8월 Telus AFS) inbox 투입 → `process-all` 정상 등록 확인

---

## 배포 이력

| 커밋 | 배포 방식 | 비고 |
|------|-----------|------|
| `d0ece2f` ~ `73bf29f` | `git push` + `vercel deploy --prod` | 웹앱 변경, 라이브 반영 완료 |
| `de3f2d5` | `git push` + `vercel deploy --prod` | 웹앱(`app/utility/page.tsx` billing_month 버그 수정) + 신규 Python 도구 커밋 |
| `d76faf4` | `git push`만 | Python 도구 전용 변경 — Vercel 배포 불필요 |

---

## 현재 DB 스키마 (이번 세션 관련)

- `utility_vendors.location_id`, `utility_locations.sort_order` 추가됨
- `user_access(email, allowed_sections text[])` 신규 테이블
- `utility_bill_imports` 신규 테이블 (감사 로그, `source_file_hash` unique)
- `utility_bills`에 대한 스키마 변경은 없음 (기존 `issue_date`/`due_date`/`previous_balance`/`current_charges`/`total_due`/`amount_paid`/`balance_status` 등 재사용)

---

## 참고 / 주의사항

- `tools/utility_bill_ingestor/.env`는 Git에 커밋되지 않음(서비스 롤 키 포함) — 새 환경에서는 `.env.example` 복사 후 직접 채워야 함
- 기본 inbox 경로: `0. DB\_inbox` (실제 OneDrive 마스터 폴더 안)
- 처리 완료(`completed`) 파일은 `0. DB\{COMPANY}\{VENDOR}\...`로 이동/개명, `needs_review`/`failed`는 로컬 `tools/utility_bill_ingestor/data/review|failed`로 이동
- Windows ARM64 환경에서는 `pip install --only-binary=:all: -r requirements.txt` 필요 (일반 설치 시 `cryptography` 소스 빌드 실패)

## 남은 잠재적 작업

- [ ] Burrtec 계정 136689184(3건) — 실제 결제 이력 확인 후 수동 정리
- [ ] Orkin/Rogers 등 예전 데이터에 남아있는 pre-existing 중복 행 정리 여부 결정 (이번 세션에서 만든 중복 아님, 그대로 둠)
- [ ] `yungyeong.j@afstransco.com` Supabase Auth 계정 생성
- [ ] `_inbox` 폴더 상시 감시가 필요하면 `tools/utility_bill_ingestor/register_watch_task.ps1`로 로그인 시 자동 시작 등록
- [ ] "리포트 메일 발송 시 Due date 기준" 기능 — 실제 코드 위치를 특정하지 못함, 사용자 확인 필요

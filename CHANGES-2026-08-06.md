# AFS Admin — Session Log (2026-08-06)

## 개요

HR 근태 대쉬보드(`app/hr/[company]/page.tsx`, `app/hr/components/AttendanceGrid.tsx`) 기능 확장 세션. 하루 다중 상태 선택, 특별휴가 코드, 잔여 연차 시간 환산, 이메일 리포트 수정 기능, 지각/조퇴 표시 기능을 순차적으로 구현·배포함.

---

## 1. 하루 다중 상태 선택 + 특별휴가(C) 코드 + 시간 환산 표시

### 커밋: `8db4343`

기존에는 하루 셀에 리브 코드 하나만 저장 가능했음 (`leave_entries` unique on `(employee_id, date)`). 완전 자유 다중 선택 방식으로 변경.

#### 구현 파일

| 파일 | 내용 |
|------|------|
| `app/hr/components/AttendanceGrid.tsx` | `leaveMap` → `Record<string, LeaveCell[]>`로 전환, `setCode` → `addEntry`/`removeEntry`/`clearCell` 3함수로 분리, 코드 선택 드롭다운을 다중 선택 UI로 재설계, `vacDisplay()`에 시간 환산 필드 추가 |
| `app/hr/components/EmployeeSearch.tsx` | `hoursLabel()` 헬퍼 추가 — 연차/병가 잔여일수 옆에 시간(8h/일 기준) 표시 |
| `app/hr/components/HrSummaryCards.tsx` | "오늘의 결근/재택" 카운트가 하루 다중 코드 중 마지막 값만 보던 버그 수정 (배열로 전체 코드 확인하도록) |
| `app/hr/[company]/page.tsx` | 이메일 리포트 `codeLabel`에 `C → Special Leave` 추가 |
| `lib/i18n.ts` | `grid.code.C` 라벨 (en/ko) 추가 |
| `supabase/allow_multi_leave_per_day.sql` | `leave_entries` unique 제약을 `(employee_id, date)` → `(employee_id, date, leave_code)`로 변경 (실행 완료) |
| `supabase/add_special_leave_code.sql` | `leave_entries_leave_code_check`에 `'C'` 코드 추가 (실행 완료) |
| `CLAUDE.md` | 리브 코드 표, Supabase 테이블 설명 업데이트 |

#### 상세

- **다중 선택**: 셀 클릭 → 드롭다운에서 여러 코드를 순서대로 추가 가능 (예: AM 반차 + PM 재택). 각 코드는 드롭다운 상단에 ✕로 개별 삭제 가능. "Clear"로 하루 전체 삭제.
- **셀 표시**: 여러 코드가 있으면 배지가 세로로 쌓여서 표시됨.
- **C 코드 (특별휴가)**: 위문 휴가 등 회사 지급 휴가. 연차 차감 없음(B 공휴일과 동일 취급). 이메일 리포트에 "Special Leave"로 포함(W/B/O는 계속 제외).
- **시간 환산**: "Leave Left" 컬럼과 직원 상세 패널의 잔여 연차/병가 옆에 `(Xh)` 형태로 8시간/일 기준 환산 표시.

---

## 2. 이메일 리포트 제목/본문 수정 가능

### 커밋: `8e71953`

기존엔 리포트 이메일 모달의 미리보기가 읽기 전용 `<pre>`였고, 발송 시에도 항상 자동 생성 텍스트를 재계산해서 보냈음 — 화면에서 수정한 내용이 반영되지 않는 버그.

#### 구현 파일

| 파일 | 내용 |
|------|------|
| `app/hr/[company]/page.tsx` | `subjectEdit`/`bodyEdit` state 추가. 미리보기를 `input`/`textarea`로 전환, 수정값을 `sendEmail()`에서 그대로 사용. 날짜 선택이나 수신자가 바뀌면 수정 내용 초기화(자동 재생성) |

---

## 3. 우클릭 지각/조퇴 표시

### 커밋: `d1ed705`

우클릭 메뉴에 기존 "메모" 기능과 별개로 지각/조퇴 표시 기능 추가. 시간(선택) + 사유(선택) 입력, 연차/병가 코드와 독립적으로 같은 날 공존 가능.

#### 구현 파일

| 파일 | 내용 |
|------|------|
| `app/hr/components/AttendanceGrid.tsx` | `flagMap` state, `saveFlag`/`deleteFlag` 함수, 우클릭 메뉴에 지각/조퇴 표시·수정·삭제 버튼 추가, 전용 모달(시간+사유), 셀에 색상 점 인디케이터(지각=빨강 좌상단, 조퇴=보라 좌하단), 툴팁에 메모+지각/조퇴 사유 통합 표시 |
| `lib/i18n.ts` | `grid.flag.*` 라벨 (en/ko) 추가 |
| `supabase/add_attendance_flags.sql` | `attendance_flags` 신규 테이블 생성 (`employee_id, date, flag_type('late'|'early_leave'), time, reason`, unique `(employee_id, date, flag_type)`), RLS 비활성화 (실행 완료 — 최초 RLS 활성화 문제로 재실행 필요했음) |
| `CLAUDE.md` | Supabase 테이블 설명에 `attendance_flags` 추가 |

---

## 배포 이력

| 커밋 | 배포 URL | 내용 |
|------|----------|------|
| `8db4343` | `hr.afstransco.com` | 다중 상태 선택 + C 코드 + 시간 환산 |
| `8e71953` | `hr.afstransco.com` | 이메일 리포트 수정 가능 |
| `d1ed705` | `hr.afstransco.com` | 지각/조퇴 우클릭 표시 |

모두 `npx vercel deploy --prod`로 배포, 3건 모두 브라우저에서 실제 저장/조회까지 확인 완료.

---

## 현재 DB 스키마 (이번 세션 관련)

```
leave_entries
  employee_id, date, leave_code, hours
  UNIQUE (employee_id, date, leave_code)   ← 이번 세션에서 변경 (기존: employee_id, date)
  leave_code CHECK 목록에 'C' 추가          ← 이번 세션에서 추가

attendance_flags                           ← 이번 세션 신규
  id, employee_id, date
  flag_type CHECK IN ('late', 'early_leave')
  time, reason, created_at
  UNIQUE (employee_id, date, flag_type)
  RLS DISABLED
```

---

## 참고 / 주의사항

- 검증 과정에서 실제 프로덕션 DB에 테스트용 임시 데이터(WFH 1건)가 잠깐 남았던 것을 확인 후 정리함.
- `attendance_flags` 테이블 최초 생성 시 RLS가 자동으로 켜져 저장이 막혔던 문제 발생 → `ALTER TABLE attendance_flags DISABLE ROW LEVEL SECURITY;` 재실행으로 해결. 이후 다른 테이블 마이그레이션 시에도 CREATE TABLE 직후 RLS 상태를 한 번 더 확인하는 게 안전함.

## 남은 잠재적 작업

- [ ] 특별휴가(C) 코드에 반차(AM/PM) 변형이 필요해지면 `C1`/`C2` 등 추가 검토
- [ ] 지각/조퇴 기록을 직원 상세 패널이나 월별 통계에 집계해서 보여주는 기능 (현재는 그리드 셀 표시만 존재)
